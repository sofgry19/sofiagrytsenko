/* Checks if browser supports javascript */
if( !document.getElementById()){
                window.location.href = "legacy.html";
}

/* Block of code that allows user to enter quiz */
function enter(){
              window.location.href = "homepage.html"
}
